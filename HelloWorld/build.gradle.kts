// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    id("com.android.application") version "8.1.1" apply false
    id("org.jetbrains.kotlin.android") version "1.8.10" apply false
}

// Este archivo contiene las opciones de
// configuración que son comunes a
// todos los módulos que componen su proyecto.
// Cada proyecto de Android Studio contiene un
// único archivo de compilación Gradle de nivel
// superior. Este archivo define los repositorios
// y dependencias de Gradle que son comunes a todos
// los módulos del proyecto.
